# Copy this table into $CODEX_HOME/config.toml, usually ~/.codex/config.toml.
# Preserve the existing configuration and merge into its single [agents] table.
# Standalone profiles need no registration here; Codex finds them in $CODEX_HOME/agents/.

[agents]
max_depth = 2
