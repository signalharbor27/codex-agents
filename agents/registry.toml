# Merge this setting into $CODEX_HOME/config.toml (normally ~/.codex/config.toml).
# Current Codex releases discover standalone profiles from $CODEX_HOME/agents/.
# Do not replace an existing config or append a duplicate [agents] table.

[agents]
max_depth = 2
